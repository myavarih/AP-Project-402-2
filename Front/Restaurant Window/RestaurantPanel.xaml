<Page x:Class="AP_Project.Front.Restaurant_Window.RestaurantPanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AP_Project.Front.Restaurant_Window"
      mc:Ignorable="d" 
      Title="RestaurantPanel">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Welcome text -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Welcome" FontSize="20" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Top"/>

        <!-- "to your Dashboard!" text -->
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Name}" FontSize="20" FontWeight="Bold" Margin="0,0,10,20" VerticalAlignment="Center"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10">
            <Button Content="Change Menu" Width="200" Height="50" Margin="5" Click="ChangeMenu_Click"/>
            <Button Content="Change Food Inventory" Width="200" Height="50" Margin="5" Click="ChangeFoodInventory_Click"/>
            <Button Content="Activate/Deactivate Reserve Service" Width="200" Height="50" Margin="5" Click="ReserveService_Click"/>
            <Button Content="Order and Reserve History" Width="200" Height="50" Margin="5" Click="OrderReserveHIstory_Click"/>
        </StackPanel>

        <!-- Logout button -->
        <Button Grid.Row="2" Grid.Column="0" Content="Logout" Width="100" Height="40" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10" Click="Logout_Click"/>
    </Grid>

</Page>
