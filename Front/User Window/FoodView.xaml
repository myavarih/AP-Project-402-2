<Page x:Class="AP_Project.Front.User_Window.FoodView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AP_Project.Front.User_Window"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1150"
      Title="FoodView">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Food Details -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
            <Image Name="FoodImage" Source="{Binding ImageName}" Width="200" Height="200" Margin="0,0,20,0"/>
            <StackPanel>
                <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Inventory: " FontSize="16"/>
                    <TextBlock Text="{Binding Inventory}" FontSize="16"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Price: " FontSize="16"/>
                    <TextBlock Text="{Binding Price, StringFormat=C}" FontSize="16"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Category: " FontSize="16"/>
                    <TextBlock Text="{Binding Category}" FontSize="16"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Rating: " FontSize="16"/>
                    <TextBlock Text="{Binding Rating}" FontSize="16"/>
                    <TextBlock Text="/5" FontSize="16"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <!-- Ingredients -->
        <TextBlock Text="Ingredients" FontSize="20" FontWeight="Bold" Grid.Row="1"/>
        <TextBlock Text="{Binding Ingredients}" FontSize="16" Grid.Row="1" Margin="0,30,0,0"/>

        <!-- Comments -->
        <StackPanel Grid.Row="2" Margin="0,30,0,20">
            <TextBlock Text="Comments" FontSize="20" FontWeight="Bold"/>
            <ListView ItemsSource="{Binding Comments}" Height="150" Margin="0,10,0,0" x:Name="ComplaintsListView">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="User" DisplayMemberBinding="{Binding UserUsername}" Width="150"/>
                        <GridViewColumn Header="Rate" DisplayMemberBinding="{Binding Rate}" Width="100"/>
                        <GridViewColumn Header="Text" DisplayMemberBinding="{Binding Text}" Width="250"/>
                        <GridViewColumn Header="Created Time" DisplayMemberBinding="{Binding CreatedTime}" Width="150"/>
                        <GridViewColumn Header="Is Edited" DisplayMemberBinding="{Binding IsEdited}" Width="100"/>
                        <GridViewColumn Header="Reply" DisplayMemberBinding="{Binding Reply}" Width="250"/>
                        <GridViewColumn Header="Edit" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Edit" Click="EditCommentButton_Click" Tag="{Binding Code}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Remove" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remove" Click="RemoveCommentButton_Click" Tag="{Binding Code}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>

        <!-- Add a Comment -->
        <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,0,0,20">
            <TextBlock Text="Add a Comment" FontSize="20" FontWeight="Bold"/>
            <TextBox Name="NewCommentTextBox" Height="60" Margin="0,10,0,0"/>
            <Button Content="Submit Comment" Width="150" Height="30" Margin="0,10,0,0" HorizontalAlignment="Left" Click="CommentSubmit_Click"/>
        </StackPanel>

        <!-- Rate this Food -->
        <StackPanel Grid.Row="4" Orientation="Vertical">
            <TextBlock Text="Rate this Food" FontSize="20" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <TextBox Name="RatingTextBox" Width="100" />
                <Button Content="Submit Rating" Width="150" Height="30" Margin="10,0,0,0" Click="SubmitRatingBtn_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
